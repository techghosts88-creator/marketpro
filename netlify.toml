[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: let React handle all routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
